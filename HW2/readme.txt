The profile page should have a link to CS page and CS page should have a link to the survey. And all of the three pages neede to be deployed in both S3 and EC2, instead you deployed the profile page in S3 and the just one page on EC2 (-3)

The survey page should be implemented in a separate page not at the end of the CS page (-2)



Visually indicate to the user which fields are required (-2)
Using the data list feature, enables the user to select high-school graduation month followed by a text field for year. (-5)
When the survey form is opened, you should automatically provide the focus in the first text field (-2)